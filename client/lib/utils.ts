import { HornerData } from "./data";

export const calculateDay = (day: number) => {
  for (const [category, bookIds] of Object.entries(HornerData)) {
    console.log(category);
  }
};

/* 
Map to each list and do something, im dumb
You go to Genesis, first is chatper 1
Then you go to History and Joshua, chapter 1
For each category you ... i dont know
*/
